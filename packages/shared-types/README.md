# shared-types

Zod ↔ Pydantic schema-parity definitions shared between `apps/web` and
`apps/api`. Empty in the scaffold phase — populated when the first agent output
schema is defined (CLAUDE.md: "Zod/Pydantic schema parity definitions").
